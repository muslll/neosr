name: inference_test
model_type: default
scale: 4
num_gpu: 1  # set num_gpu: 0 for cpu mode
manual_seed: 0

datasets:
  test_1:  # the 1st test dataset
    name: Set5
    type: single
    dataroot_lq: /home/anon/Downloads/validation/testset_div2k_val_small/lq/
    io_backend:
      type: disk

# network structures
network_g:
  type: compact
  num_in_ch: 3
  num_out_ch: 3
  num_feat: 64
  num_conv: 16
  upscale: 4
  act_type: prelu
    #conv2x2: False

# path
path:
  pretrain_network_g: /tmp/4x_rybu.pth
  #param_key_g: params_ema
  strict_load_g: true

# validation settings
val:
  save_img: true
  suffix: ~  # add suffix to saved images, if None, use exp name
